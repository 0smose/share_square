<!-- Begining of the loop -->
<% @users.order(created_at: :asc).each do |u| %>
<!-- CARD -->
<%= link_to(edit_admins_ad_path(a.id), class: "text-default") do %>
<div class="card flex-md-row no-gutters mx-2 mt-3 o-hidden">
	<div class="card-body d-flex flex-column col-auto p-3">
		<div class="d-flex justify-content-between mb-2">
			<div class="text-small d-flex">
				<div class="mr-2 text-primary">
					<%= "Admin: #{u.is_admin}" %>
				</div>
				
			</div>
		</div>
		<div class="flex-grow-1">
			<div class="d-flex align-items-center">
				<%= user_avatar(u.user.id, "avatar avatar-sm") %>
				<div class="ml-1">
					<span class="text-small"><%= u.user.first_name %> <%= u.user.last_name %></span>
				</div>
			</div>
		</div>
	</div>
</div>
<% end %>
<% end %>





<div class="col col-5 card my-2 mx-auto p-3 shadow">
	<!-- We display each user in the dashboard -->
	<p>
		<%= "ID: #{u.id}" %><hr>				
		<p><%= u.first_name %>
			<%= "#{u.last_name}" %></p>
			<p><%= u.email %></p>
			
		</p>	
		<%= link_to "Voir plus de détails", admins_user_path(u.id), class: "btn btn-primary" %>	
	</p>	
</div> 
<% end %>